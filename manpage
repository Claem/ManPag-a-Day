#!/usr/bin/env python

import commands
f = open('filelist', 'r')
searchterm = f.readline().strip()
f.close()
print searchterm

cmd = 'man %s' % (searchterm)
s = commands.getoutput(cmd) #This line makes it a callable function.
print(s)
